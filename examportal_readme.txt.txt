
//Frontend code setup
1.open project in vs code, if node modules are not there 2then install node modules otherwise skip
node modules installation

npm install--legacy-peer-deps

//openssl issue solving
$env = NODE_OPTIONS='--openssl-legacy-provider'
//css issue solving
npm update --legacy-peer-deps

//run below command to run project
ng serve

open localhost:4200 in web browser like chrome, then we will access client application

//Backend setup
1.open project in eclipse
